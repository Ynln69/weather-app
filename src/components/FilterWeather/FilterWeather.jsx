import { Thumb, Input, Button } from "./FilterWeather.styled";

const FilterWeather = () => {
  return (
    <Thumb>
      <Input type="text" />
      <Button>Add</Button>
    </Thumb>
  );
};

export default FilterWeather;
